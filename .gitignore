__pycache__/
*.pyc
.venv/
.DS_Store
.env

# Obsidian vault personal config (workspace state, plugin layout, etc.)
.obsidian/

# Research raw artifacts (intermediate data, not canonical)
research/raw_scrape_*.txt
research/raw_kaggle_full_output.txt
research/nb*_content.txt
research/batch*_results.md
research/fetch_notebooks.py
research/scrape_*.md
research/scraped_*.md

# Local canonical research stash (107 files, 2 MB).
# See AGENTS.md "Research Directory" rules. Files preserved on disk
# but not version-controlled to keep repo lean. Promote individual
# files to docs/ if/when they become load-bearing for a contributor.
docs/research/research/

# Bind-mount target for the Dockerized hermetic stub
# (tests/fixtures/claude-stub/Dockerfile). Captured request bodies +
# access logs land here when the operator runs the stub container.
# Per-machine debug artifacts — never committed.
.stub-logs/
results.json
text_*.json
test.log
_dep-test/
user_resource_dump/

# Local Claude Code config (operator-specific)
.claude/
