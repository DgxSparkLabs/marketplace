# Marketplace inventory

_Generated by `scripts/generate_manifest.py` — do not edit by hand. This is the authoritative plugin list; reference it instead of hardcoding counts in prose (README, CONTRIBUTING, HANDOFF)._

**27 plugin entries**: 2 agent, 1 bundle, 2 command, 10 hook, 2 lsp, 2 mcp, 2 monitor, 2 output-style, 2 skill, 2 theme

## agent (2)
- `agent-example-multi` — Reviews a lab notebook entry as a skeptical peer reviewer. Use when the user has drafted a notebook entry and wants a critical second opinion before publishing.
- `agent-example-single` — Reviews a lab notebook entry as a skeptical peer reviewer. Use when the user has drafted a notebook entry and wants a critical second opinion before publishing.

## bundle (1)
- `bundle-examples` — Reference examples — install to study every construct type. Each Claude-supported construct ships both example-single and example-multi (hooks ship nine per-event plus example-multi).

## command (2)
- `command-example-multi` — Prints a formatted lab-notebook closing footer with a UTC timestamp.
- `command-example-single` — Reference example slash command. Prints a formatted lab-notebook header.

## hook (10)
- `hook-example-multi` — Reference example hook covering all nine Claude hook events. Each handler reads the hook's JSON payload from stdin and appends a timestamped marker PLUS that full payload to /tmp/hook-fired-<event>.log — a 'debug example' so authors can see exactly what Claude sends each event (verify with `tail /tmp/hook-fired-*.log`, or `cat … | tail -n +1` and pipe through `jq` to pretty-print). The UserPromptSubmit handler also injects a context line into the prompt (stdout).
- `hook-example-notification` — Single-event reference hook for Notification. Fires when Claude wants to alert the user (e.g. tool-permission prompts); appends a timestamped marker plus the full hook JSON payload (read from stdin) to /tmp/hook-fired-notification.log.
- `hook-example-posttooluse` — Single-event reference hook for PostToolUse, scoped to Write|Edit. After the tool completes, appends a timestamped marker plus the full hook JSON payload (read from stdin) to /tmp/hook-fired-posttooluse.log. The payload carries the tool name, input, and response — read it from stdin JSON, NOT from a CLAUDE_TOOL_NAME env var (which is not populated).
- `hook-example-precompact` — Single-event reference hook for PreCompact. Fires before Claude compacts conversation history; appends a timestamped marker plus the full hook JSON payload (read from stdin) to /tmp/hook-fired-precompact.log.
- `hook-example-pretooluse` — Single-event reference hook for PreToolUse, scoped to Write|Edit. Before the tool runs, appends a timestamped marker plus the full hook JSON payload (read from stdin) to /tmp/hook-fired-pretooluse.log. The payload carries the tool name and tool input — read it from stdin JSON, NOT from a CLAUDE_TOOL_NAME env var (which is not populated).
- `hook-example-sessionend` — Single-event reference hook for SessionEnd. Fires once when a Claude session terminates; appends a timestamped marker plus the full hook JSON payload (read from stdin) to /tmp/hook-fired-sessionend.log.
- `hook-example-sessionstart` — Single-event reference hook for SessionStart. Fires once when a Claude session begins; appends a timestamped marker PLUS the full hook JSON payload (read from stdin) to /tmp/hook-fired-sessionstart.log so authors can see exactly what Claude sends a hook.
- `hook-example-stop` — Single-event reference hook for Stop. Fires when the assistant ends a turn; appends a timestamped marker plus the full hook JSON payload (read from stdin) to /tmp/hook-fired-stop.log.
- `hook-example-subagentstop` — Single-event reference hook for SubagentStop. Fires when a sub-agent's turn ends; appends a timestamped marker plus the full hook JSON payload (read from stdin) to /tmp/hook-fired-subagentstop.log.
- `hook-example-userpromptsubmit` — Single-event reference hook for UserPromptSubmit. Injects a [Lab Notebook context: timestamp=...] line into the prompt (stdout) AND appends a timestamped marker plus the full hook JSON payload (read from stdin) to /tmp/hook-fired-userpromptsubmit.log for debugging.

## lsp (2)
- `lsp-example-multi` — Two bundled, dependency-free reference LSP servers (markdown + python) implementing real language-server features — diagnostics, document symbols, go-to-definition, hover, and references. Works out of the box (only uv required); no external language-server binary to install.
- `lsp-example-single` — Reference template for an LSP server plugin — copy to scaffold your own.

## mcp (2)
- `mcp-example-multi` — Three reference MCP servers demonstrating the multi-server plugin layout.
- `mcp-example-single` — Reference template for an MCP server plugin — copy to scaffold your own.

## monitor (2)
- `monitor-example-multi` — Three reference monitors demonstrating the multi-monitor plugin layout.
- `monitor-example-single` — Reference template for a background monitor plugin — copy to scaffold your own.

## output-style (2)
- `output-style-example-multi` — Three reference output styles demonstrating the multi-style plugin layout.
- `output-style-example-single` — Reference template for an output style plugin — copy to scaffold your own.

## skill (2)
- `skill-example-multi` — Two reference skills demonstrating the multi-skill plugin layout.
- `skill-example-single` — Prints a minimal greeting plus a UTC timestamp. The smallest possible single-skill reference plugin.

## theme (2)
- `theme-example-multi` — Three reference themes demonstrating the multi-theme plugin layout.
- `theme-example-single` — Reference template for a theme plugin (experimental) — copy to scaffold your own.
