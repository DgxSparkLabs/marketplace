# catalog.toml — bundle definitions ONLY
#
# After the DI refactor (see docs/archive/di-refactor/PLAN_DI_REFACTOR.md), this file contains
# ONLY [bundle.*] blocks. Construct types, platforms, and catch-all bundles
# are defined in code (scripts/constructs.py, scripts/platforms.py,
# scripts/generate_manifest.py Phase 2b) — not in configuration files.
#
# Bundle member syntax: "<kind>:<name>"
#   skill:telegram-notify       → individual plugin "skill-telegram-notify"
#   rule:no-ai-credit           → individual plugin "rule-no-ai-credit"
#   bundle:communication-skills → bundle plugin "bundle-communication-skills"
#
# Reserved names: <prefix>-all (e.g., skill-all, rule-all) are auto-generated
# by Phase 2b of the generator. Defining them here raises a ValueError.
#
# Then run: uv run scripts/generate_manifest.py

# ─── Skill domain bundles (migrated from [skill_domain.*]) ───────────────────

[bundle.search-research-skills]
description = "Find information across web, code repos, and video"
members = [
  "skill:duckduckgo-search",
  "skill:github-search",
  "skill:web-scraper",
  "skill:youtube-search",
  "skill:youtube-wisdom",
]

[bundle.communication-skills]
description = "Send emails, messages, and notifications"
members = [
  "skill:send-email",
  "skill:telegram-notify",
]

[bundle.devops-skills]
description = "CI/CD, repository setup, port tunneling, infrastructure tools"
members = [
  "skill:act-runner",
  "skill:expose-port",
  "skill:github-repo-setup",
  "skill:ssh-tunnel",
]

[bundle.session-management-skills]
description = "Mid-session realignment, motivation, recall, history, audits"
members = [
  "skill:check",
  "skill:motivation",
  "skill:recall-rules",
  "skill:session-wrapup",
  "skill:session-history",
  "skill:pitfall-check",
]

[bundle.project-scaffolding-skills]
description = "Initialize project documentation and structured handoffs"
members = [
  "skill:project-bootstrap",
  "skill:structured-handoff",
]

[bundle.code-analysis-skills]
description = "Codebase audits and domain-specific reference guides"
members = [
  "skill:code-health-audit",
  "skill:textual-tui-guide",
]

[bundle.meta-tooling-skills]
description = "Skill creation, analytics, and configuration synchronization"
members = [
  "skill:skill-creator",
  "skill:skill-analytics",
  "skill:sync-rules",
]

[bundle.ai-services-skills]
description = "External AI APIs and cloud service integrations"
members = [
  "skill:gemini-chat",
  "skill:google-drive-reader",
]

# ─── Rule domain bundles (migrated from [rule_domain.*]) ─────────────────────

[bundle.quality-rules]
description = "Code quality, testing, verification, and review discipline"
members = [
  "rule:blast-radius",
  "rule:code-hygiene",
  "rule:pitfalls-discipline",
  "rule:prior-art",
  "rule:simplicity-bar",
  "rule:verification-ladder",
  "rule:verify-your-work",
]

[bundle.workflow-rules]
description = "How agents approach, decompose, and execute tasks"
members = [
  "rule:autonomous-persistence",
  "rule:continuous-improvement",
  "rule:improve-the-process",
  "rule:revert-on-failure",
  "rule:session-resilience",
  "rule:stay-motivated",
  "rule:task-formation",
]

[bundle.documentation-rules]
description = "Tracking work, documenting decisions, maintaining project history"
members = [
  "rule:document-lifecycle",
  "rule:document-progress",
  "rule:readable-docs",
]

[bundle.environment-rules]
description = "Tooling conventions and output discipline"
members = [
  "rule:no-ai-credit",
  "rule:output-discipline",
  "rule:python-uv",
]

[bundle.notifications-rules]
description = "Alert hooks for important events"
members = [
  "rule:telegram-on-complete",
]

# ─── Per-construct catch-all bundles are NOT declared here ───────────────────
#
# Decision #23: bundle-<prefix>-all (skill-all, rule-all, ..., theme-all)
# is generated by the code (Phase 2b of generate_manifest.py), NOT
# declared in catalog.toml. They have no user-configurable content —
# every catch-all is "every member of that construct" — so configuration
# files have nothing useful to contribute. The generator owns the 10
# reserved names and emits the bundles directly.
#
# Decision #25: load_bundles raises ValueError if catalog tries to define
# a reserved name (e.g., [bundle.skill-all] here raises an error).

# ─── Cross-construct examples bundle ─────────────────────────────────────────

[bundle.examples]
description = "Reference examples — install to study every construct type"
members = [
  "skill:example",
  "rule:example",
  "command:example",
  "agent:example",
  "hook:example",
  "mcp:example",
  "lsp:example",
  "monitor:example",
  "output-style:example",
  "theme:example",
]
